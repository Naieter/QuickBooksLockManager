<Window x:Class="QBLockServiceSetup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickBooks Lock Service — Setup"
        Width="540" Height="456"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0f172a">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="#60a5fa"/>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563eb"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#334155"/>
                                <Setter Property="Foreground" Value="#64748b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
        </Style>
        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#dc2626"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="28,24,28,24">

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="QuickBooks Lock Service" FontSize="20" FontWeight="Bold"
                       Foreground="#60a5fa"/>
            <TextBlock x:Name="VersionText" FontSize="12" Foreground="#475569"
                       VerticalAlignment="Bottom" Margin="8,0,0,2"/>
        </StackPanel>
        <TextBlock Text="Server Installer" FontSize="12" Foreground="#64748b" Margin="0,0,0,16"/>

        <!-- Status banner -->
        <Border x:Name="BannerBorder" CornerRadius="4" Padding="12,8" Margin="0,0,0,18">
            <TextBlock x:Name="BannerText" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Config grid -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
                <RowDefinition Height="36"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="API Key" Style="{StaticResource Label}"/>
            <TextBox   Grid.Row="0" Grid.Column="1" x:Name="ApiKeyBox"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Port" Style="{StaticResource Label}"/>
            <TextBox   Grid.Row="1" Grid.Column="1" x:Name="PortBox" Text="5100"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Install Directory" Style="{StaticResource Label}"/>
            <TextBox   Grid.Row="2" Grid.Column="1" x:Name="InstallDirBox" Text="C:\QBLockService"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Lock Timeout (min)" Style="{StaticResource Label}"/>
            <TextBox   Grid.Row="3" Grid.Column="1" x:Name="TimeoutBox" Text="5"/>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource SecondaryBtn}" Content="Generate Keys"
                    Click="GenerateKeys_Click" Margin="0,0,8,0"/>
            <Button x:Name="UninstallButton" Style="{StaticResource DangerBtn}"
                    Content="Uninstall" Click="Uninstall_Click"
                    Margin="0,0,8,0" Visibility="Collapsed"/>
            <Button x:Name="InstallButton" Style="{StaticResource PrimaryBtn}"
                    Content="Install" Click="Install_Click"/>
        </StackPanel>

        <!-- Log -->
        <Border Background="#1e293b" BorderBrush="#334155" BorderThickness="1"
                CornerRadius="4" Padding="10" Margin="0,16,0,0" Height="100">
            <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogText" FontFamily="Consolas" FontSize="11"
                           Foreground="#94a3b8" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

    </StackPanel>
</Window>
